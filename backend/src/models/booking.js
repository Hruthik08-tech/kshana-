/*
 * Booking Model
 * Data access layer for the bookings table.
 * Connects to: MySQL via src/config/db.js
 * Owner: Backend Team
 */

const db = require('../config/db');

// TODO: create(bookingData) — insert a new booking with seat and user references
// TODO: findByUser(userId) — fetch all bookings belonging to a user
// TODO: findById(bookingId) — fetch single booking by primary key
// TODO: cancel(bookingId) — mark booking as cancelled, release seats

module.exports = {
  // Export model functions here once implemented
};
