/*
 * Seat Model
 * Data access layer for the seats table (event venue seating).
 * Connects to: MySQL via src/config/db.js
 * Owner: Backend Team
 */

const db = require('../config/db');

// TODO: findByEvent(eventId) — fetch all seats for an event
// TODO: reserve(seatId, bookingId) — mark a seat as reserved
// TODO: release(seatId) — free a previously reserved seat
// TODO: getAvailability(eventId) — count available vs total seats

module.exports = {
  // Export model functions here once implemented
};
